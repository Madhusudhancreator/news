name = "newspaper"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
main = ".open-next/worker.js"

[assets]
directory = ".open-next/assets"
binding = "ASSETS"

[vars]
NODE_ENV = "production"

# DATABASE_URL is stored as a secret — set it with:
# npx wrangler secret put DATABASE_URL
